# server-statistics
Metabase instance for tracking website statistics using postgres


### Setup

1. Copy `.env.example` → `.env`
2. Update credentials in `.env`
3. Run:
   ```bash
   docker compose up -d